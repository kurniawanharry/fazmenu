enum NavigationPanel {
  order,
  history,
  product,
  category,
  discount,
  addition,
  analytics,
  payment,
  settings,
}

enum FieldTheme { dark, light }

enum ProductType { unit, package }

enum ProductStock { always, setting }

enum HistoryDate { today, yesterday, pastSevenDay, pasThirtyDay, adjust }
