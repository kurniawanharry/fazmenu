enum NavigationPanel {
  order,
  history,
  product,
  category,
  discount,
  addition,
  analytics,
  payment,
  settings,
}

enum ProgressIndicatorType {
  rotatingPlain,
  doubleBounce,
  wave,
  wanderingCubes,
  fadingFour,
  fadingCube,
  pulse,
  chasingDots,
  threeBounce,
  circle,
  cubeGrid,
  fadingCircle,
  rotatingCircle,
  foldingCube,
  pumpingHeart,
  dualRing,
  hourGlass,
  pouringHourGlass,
  fadingGrid,
  ring,
  ripple,
  spinningCircle,
  squareCircle
}

enum FieldTheme { dark, light }

enum ProductType { unit, package }

enum ProductStock { always, setting }

enum HistoryDate { today, yesterday, pastSevenDay, pasThirtyDay, adjust }

enum ServiceDate { fifteen, thirty, fourty, adjust }

enum OrderType { ongoing, success, cancel }

enum OpsiProduct { one, more }

enum OpsiRequired { optional, required }

enum CategoryMeta { food, drink, other }

enum DialogIcon { ok, cancel, question }

enum DetailRecommend { auto, manual }

enum OrderRecommend { auto, manual }
